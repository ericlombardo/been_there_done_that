<% content_for :title do %> <!--collect title for banner-->
  <% if profile_creator? %> 
    <h1>Welcome Back <%=@user.username %></h1>
  <% else %>
    <h1><%=@user.username %>'s Profile</h1>
  <% end %>
<% end %>

<div class="padded_sides text-center"> 
  <div class="row">   <!--row that shows top activites and states-->
    <div class="col">
      <h2>Most Visited States</h2>
      <% @top_states.each do |s| %>
        <h4><%="#{s[0]} - #{s[1]}"%></h4>
      <% end %>
    </div>
    <div class="col">
      <h2>Favorite Activites</h2>
      <% @top_activities.each do |a| %>
        <h4><%="#{a[0]} - #{a[1]}"%></h4>
      <% end %>
    </div>
  </div><br>
  <div class="row">   <!--row that shows states and count-->
    <div class="col">
      <h2>States Visited - <%=@state_list.count%> / 50</h2>
      <h5><%=@state_list.join(', ')%></h5>
    </div>
  </div><br>
  <div class="row">   <!--row that shows activities and count-->
    <div class="col">
      <h2>Activity Count - <%=@activity_list.count%></h2>
      <h5><%=@activity_list.join(', ')%></h5>
    </div>
  </div>

  <br><br><br>
  <% if profile_creator? %>   <!--shows delete profile button if correct user-->
    <form method="POST" action="/users/<%=current_user.slug %>">
      <input id="hidden" type="hidden" name="_method" value="DELETE">
      <button class="btn btn-danger">Delete Profile</button>
    </form>
  <% end %>
</div>